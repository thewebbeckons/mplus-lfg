# Copy to .dev.vars for `wrangler dev`. Values come from
# https://discord.com/developers/applications -> your app.
#
# General Information -> Public Key
DISCORD_PUBLIC_KEY=""
# General Information -> Application ID
DISCORD_APPLICATION_ID=""
# Bot -> Token (only used by the cron sweep to edit expired run messages)
DISCORD_TOKEN=""

# Optional: register the slash command to a single guild for instant availability.
# Global registration can take up to an hour to propagate.
DISCORD_GUILD_ID=""
